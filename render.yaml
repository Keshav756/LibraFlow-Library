services:
  - type: web
    name: libraflow-backend
    env: node
    plan: free
    buildCommand: npm install && cd server && npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 18